# e2e room reuse. Copy to .env and fill with existing conference links.
# Empty room = platform is SKIPPED (harness refuses to create a new call).
# To allow creating fresh rooms per run, set ALLOW_NEW_ROOMS=1.

ROOM_TELEMOST=""
ROOM_WBSTREAM=""
ROOM_DION=""
ROOM_BITRIX=""

ALLOW_NEW_ROOMS=0
